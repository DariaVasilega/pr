{% extends 'base.html.twig' %}
{% block title %}Admin Menu{% endblock %}
{% block body %}
    <div class="container w-50">
        {{ form_start(form, {'action': path('admin_set_verification', {'method':'POST'})}) }}
            <div class="d-flex flex-row justify-content-between">
                <div class="w-75">
                    {{ form_row(form.users) }}
                </div>
                <div class="d-flex flex-column justify-content-between">
                    <div class="d-flex flex-row justify-content-start">
                        <div class="m-3"></div>
                                {{ form_row(form.choice) }}
                    </div>
                    <button class="btn btn-success" type="submit">CHANGE DATA</button>
                </div>
            </div>
        {{ form_end(form) }}
        <div class="m-5"></div>
        <a class="btn btn-block btn-dark" href="{{ path('profile_index', {'id': app.user.id, 'page': thisPage}) }}">Back</a>
    </div>
{% endblock %}